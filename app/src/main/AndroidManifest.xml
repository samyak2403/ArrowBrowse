<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.arrowwould.arrowbrowse">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/browser"
        android:label="@string/app_name"
        android:roundIcon="@drawable/browser"
        android:supportsRtl="true"
        android:theme="@style/Theme.LetsBrowse"
        android:usesCleartextTraffic="true"
        tools:targetApi="m">
        <activity
            android:name="com.arrowwould.arrowbrowse.activity.BookmarkActivity"
            android:exported="false" />
        <activity
            android:name="com.arrowwould.arrowbrowse.activity.MainActivity"
            android:configChanges="uiMode|keyboard|keyboardHidden|screenSize|screenLayout|smallestScreenSize|orientation"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>